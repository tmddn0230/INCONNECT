using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Motion Data 를 제외한 패킷 리시버
public class ICPacketReciever : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
